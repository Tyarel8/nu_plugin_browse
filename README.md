# Http browse plugin

A nushell plugin that makes http request through a headless browser using [chromiumoxide](https://github.com/mattsse/chromiumoxide).

Requires chrome/chromium to be installed.

## Installation
```sh
cargo install nu_plugin_browse
plugin add ~/.cargo/bin/nu_plugin_browse
```

## Usage

run `http browse --help` to get the parameters and examples.
